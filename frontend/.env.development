# The app's API paths (e.g. /auth/register-company) are relative to the /api base.
# Vite's proxy serves /api on the dev server, and the backend exposes /api too,
# so point VITE_API_URL at the full base including the /api prefix.
VITE_API_URL=http://localhost:5179/api